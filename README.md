# Password-Manager
A Basic Password Manager built using Python as the frontend and MySQL as the backend to store user credentials. It allows users to add, view, update, delete, and search login details for different websites, demonstrating Python–MySQL connectivity
